//
// The inventory information in AdEngine
// 

option optimize_for = SPEED;

package rubicon.data.ad_engine;

import "num_types.proto";

//
// inventory info
//
message Inventory_PB {
    optional rubicon.data.common.AnUInt32_PB    account_id      = 1;  
    optional rubicon.data.common.AnUInt32_PB    site_id         = 2; 
    optional rubicon.data.common.AnUInt32_PB    zone_id         = 3;          
    optional rubicon.data.common.AnUInt32_PB    size_id         = 4;
    optional rubicon.data.common.AnUInt32_PB    zonesize_id     = 5;  // "zonesize", if not in "zone-size" format (legacy stuff)(only used for ae_log)
    optional string                             zone_size_str   = 6;  // value of "zone-size" or "zonesize" in string
    optional rubicon.data.common.AnUInt32_PB    org_zone_id     = 7;  // original zone_id in the request uri (if original is changed in rules file)          
    optional rubicon.data.common.AnUInt32_PB    org_size_id     = 8;  // original size_id in the request uri (if original is changed in rules file)
}

